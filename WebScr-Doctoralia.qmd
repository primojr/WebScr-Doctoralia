---
title: "Analise exploratória - Doctoralia"
subtitle: "Especialidade - Psiquiatria"
format: html
editor: visual
---

## Objetivo

Fazer uma analise exploratória da especialidade de Psiquiatra no Doctoralia

-   Densidade
-   Preço médio cobrado

## EDA - Analise exploratória

### Bibliotecas

```{python}
import pandas as pd

```

```{python}

```
When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{python}

```
```{python}
#| echo: false

df = pd.read_json('./dados/output.jl', lines=True)

df.head(10)

```

-   Numero de psiquiatras por estado.

```{python}
df.groupby(df['region']) \
 .size()\
 .reset_index(name = 'qtd') \
 .sort_values(by = "qtd", ascreseng )

```

```{python}

df["city1"].value_counts()

df['price'] = pd.to_numeric(df['price'], errors="coerce")

lista_cidades = [
  "São José dos Campos",
  "Salvador",
  "São Paulo",
  "Goiânia",
  "Guanambi",
  "Palmas",
  "São Paulo",
  "Feira de Santana",
  "Lauro de Freitas"
  ]

df2 = df.loc[df['city1'].isin(lista_cidades)]
df2 = df2.loc[df2['price'] >= 100]

(df2
 .groupby(df2['city1'])
 .agg({"price":"describe"})
)
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).

## Referencia

-   [Extração dos dados](https://github.com/corralm/doctoralia-scraper/tree/main)
-   \[\]